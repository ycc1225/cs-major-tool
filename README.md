# 锦标赛管理工具 (Tournament Management Tool)

一个可配置的、通用的赛事管理工具，使用React、TypeScript和可插拔的策略引擎。最初为CS Major瑞士轮设计，但可扩展以支持其他游戏或体育赛事。

## 🌟 核心功能

* **可配置的赛制:** 支持瑞士轮 (Swiss System)、单败淘汰赛 (Single Elimination) 等。
* **策略引擎:** 配对 (Pairing) 和排名 (Ranking) 逻辑是可插拔的 (Strategy Pattern)。
* **"What-if" 模式:** 允许用户实时模拟和预测比赛结果。
* **类型安全:** 完全使用 TypeScript 编写，具有清晰的数据模型。

## 🛠️ 技术栈

* **UI:** React + Vite
* **语言:** TypeScript
* **状态管理:** Zustand
* **核心引擎:** 纯 TypeScript (与UI分离)
* **代码规范:** ESLint + Prettier

## 🚀 快速开始

1. **安装依赖:**

    ```bash
    npm install
    ```

2. **启动开发服务器:**

    ```bash
    npm run dev
    ```

## 📁 项目结构

* `/src`
  * `/components/` - (通用 UI 组件)
  * `/config/` - (锦标赛配置文件, JSON)
  * `/core/` - (核心引擎, 纯TS, 算法)
    * `/strategies/` - (配对/排名策略)
  * `/features/` - (智能组件, 业务视图)
  * `/store/` - (Zustand 状态)
  * `/types/` - (核心数据模型, domain.ts)
